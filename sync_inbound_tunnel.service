[Unit]
Description=sync x-ui tunnel inbound traffic
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/bin/env python3 /usr/local/bin/sync_inbound_tunnel.py \
          --db /etc/x-ui/x-ui.db \
          --interval 30 \
          --apply \
          --backup
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target
